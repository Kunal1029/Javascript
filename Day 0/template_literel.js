let k = 12;
let p = 12;
console.log(`Sum of k ${k} and p ${p} is ${k+p}`); // we don't need to concate here
console.log(`they used to add embedded expressions in a string`)

//Template Literals use back-ticks (``) rather than the quotes ("") to define a string:



