//Funnction with arguments

//syntax
function funcName(arg1,arg2){
    console.log(arg1,arg2);
}
funcName("Argument one, ", "Argument two");

function info(name,age){
    console.log(`${name}'s age is ${age}`);
}
info("Kunal",23)
info("Kunal")
info(23)

//if we give only one parameter but function needs two parameter so only 1st parameter will be store